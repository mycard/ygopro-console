// Generated by CoffeeScript 2.1.0
import React, {
  Component
} from 'react';

import {
  Panel,
  ListGroup,
  ListGroupItem
} from 'react-bootstrap';

export var MCProConsoleIdentifierResult = class MCProConsoleIdentifierResult extends Component {
  constructor() {
    super();
  }

  componentDidMount() {}

  renderDeck(result) {
    if (result.polymerizedTags) {
      return <div>
                聚合&nbsp;
                {result.polymerizedTags.map((tag) => {
        return <kbd>{tag}</kbd>;
      })} 
            </div>;
    }
    return <div>{this.props.result.deck}</div>;
  }

  render() {
    if (!this.props.result) {
      return null;
    }
    return <Panel header="检验结果">
            {this.renderDeck(this.props.result)}
            {(this.props.result.tag ? <ListGroup fill>
                        {this.props.result.tag.map((tag) => {
      return <ListGroupItem>{tag}</ListGroupItem>;
    })}
                    </ListGroup> : null)}
        </Panel>;
  }

};

MCProConsoleIdentifierResult.defaultProps = {
  result: null
};

export default MCProConsoleIdentifierResult;

//# sourceMappingURL=result.js.map
